---
description: Enforces Figma traceability metadata in component markdown pages to reduce documentation drift.
globs:
  - "docs/components/*.md"
---

# Component Figma traceability

This rule applies to component pages in `docs/components/` (excluding `overview.md`).

## Required frontmatter contract

Component docs must include this metadata block:

```md
---
doc_type: component
doc_status: draft # draft | ready | needs-review
figma:
  file_url: "https://www.figma.com/design/<file>/<name>?node-id=<node>"
  page: "<FigmaPageName>"
  component: "<ComponentOrSetName>"
  component_set_node_id: "123:456" # optional, but if present must match linked spec
  last_verified: "YYYY-MM-DD"
  component_hash: "<sha256>" # optional deterministic snapshot hash from the verified component spec
  properties_count: 0 # optional non-negative integer
  variants_count: 0 # optional non-negative integer
---
```

If a value is unknown, use `TBD` instead of guessing.

## Traceability rules

- `figma.component` must use a human-readable component/set name, not node IDs.
- `figma.page` should match the page name in Figma.
- `figma.last_verified` records the latest manual verification date.
- Keep a direct source link in `## Overview` (human-readable URL text is fine).
- Include `node-id` in the source URL when available to preserve exact node traceability.
- If `figma.component_set_node_id` is present, it must be valid (`123:456`) and match the linked component spec value.
- Spec YAML is the canonical source for `component_set_node_id`; markdown may mirror it for readability.
- If `figma.component_hash` is present, it must be a 64-char SHA-256 hex string.
- If present, `figma.properties_count` and `figma.variants_count` must be non-negative integers.
- For deterministic maintenance, these optional fields should be regenerated from the linked component spec instead of edited manually.

## Prohibited values

- Do not use `VariableID:*` as metadata.
- Node IDs are allowed in traceability URLs and metadata notes.
- Do not use node IDs as token identifiers.
