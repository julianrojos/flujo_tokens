name: CardIconContainer
status: draft
figma:
  file: 3hGC1ju0d5AKzaoI9pKIyu
  page: Cards
  component_set: Card-Icon-Container
  component_set_node_id: '2053:7862'
summary:
  purpose: Documentation scaffold for Card Icon Container extracted from Figma component set metadata.
  when_to_use: Use Card Icon Container when this pattern is needed in the interface and matches its Figma variants.
  when_not_to_use: Do not use Card Icon Container for scenarios not represented by its current variant/property contract.
anatomy:
  - id: container
    description: Primary visual container for Card Icon Container.
  - id: content
    description: Child content area controlled by text/slot/component properties.
properties:
  - name: Background_Color
    type: enum
    default: Background_Color1
    required: true
    description: Variant selector extracted from Figma property `Background_Color`.
    values:
      - Background_Color1
      - Background_Color2
      - Background_Color3
      - Background_Color4
      - Background_Color5
      - Background_Color6
  - name: Change_Icon
    type: instance_swap
    default: '65:710'
    required: false
    description: Property extracted from Figma property `Change_Icon`.
content_guidelines:
  rules:
    - Keep content concise and aligned with product voice.
    - Do not exceed space available in the default variant without overflow handling.
best_practices:
  do:
    - Use the variant and property contract exactly as defined in Figma.
    - Validate visual behavior before promoting to ready status.
  dont:
    - Do not overload this component with semantics it was not designed for.
    - Do not hardcode colors or spacing outside the token system.
accessibility:
  role: TBD
  focus:
    tokens:
      inner: Semantic.Color.Focus-Outline.Inner
      outer: Semantic.Color.Focus-Outline.Outer
  hit_area:
    desktop_token: A11y.A11y.Dimension.Min-Hit-Area
    mobile_token: Primitives.Dimension.A11y.Min-Hit-Area-Mobile-AAA
  labeling:
    rules:
      - Provide an accessible name when interactive.
      - Avoid redundant announcements for decorative content.
token_mapping:
  container.background:
    background_color=Background_Color1: TBD
    background_color=Background_Color2: TBD
    background_color=Background_Color3: TBD
    background_color=Background_Color4: TBD
    background_color=Background_Color5: TBD
    background_color=Background_Color6: TBD
qa:
  - Properties match the Figma component set.
  - Token mapping uses real token keys or explicit TBD values pending verification.
related_components: []
