---
description: Keeps section overview/index pages synchronized when component docs change.
globs:
  - "docs/components/*.md"
  - "docs/**/overview.md"
---

# Overview and index maintenance

## Mandatory synchronization

When a component page is created, deleted, or renamed, update its overview page in the same change.

## Components

- Source pages: `docs/components/*.md` (excluding `overview.md`)
- Index page: `docs/components/overview.md`
- The component list MUST follow canonical format:
  - `- [Display Name](snake_case.md)`
- The component list MUST be sorted alphabetically by display name (case-insensitive).
- Ties MUST be resolved by `snake_case.md` ascending.
- Ensure links point to existing files.
- For exact format/order/uniqueness constraints, follow `overview-components-canonical-list.mdc`.

## Consistency checks

- No orphan docs (page exists but not listed in overview).
- No dead links in overview pages.
- One entry per page (no duplicates).
